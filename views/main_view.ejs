<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
    <h1>Hello <%= name %></h1>
    <% dogs.forEach(dog => { %>
        <h2><%= dog.toUpperCase() %></h2>

    <% }) %>
    <% for (let i = 0; i < num; i++) { %>
        <p><%= i %></p>
    <% } %>
</body>
</html>